@model OnTheSpot.Models.Order

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Order</legend>

    <table>

        <tr>
            <td>@Html.LabelFor(model => model.OrderID)</td>
            <td>@Html.DisplayFor(model => model.OrderID)</td>
        </tr>
        <tr>
            <td>@Html.LabelFor(model => model.UserName)</td>
            <td>@Html.DisplayFor(model => model.UserName)</td>
        </tr>
        <tr>
            <td>@Html.LabelFor(model => model.OrderSubmitted)</td>
            <td>@Html.DisplayFor(model => model.OrderSubmitted)</td>
        </tr>

    </table>

</fieldset>
@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
